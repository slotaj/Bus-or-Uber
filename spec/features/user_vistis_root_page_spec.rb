require 'rails_helper'

RSpec.describe "the big picture of whats happening", type: :feature do
  context "next level describing whats happening" do
    scenario "last level of whats going on" do
        assert_equal 2, 1+1
    end
  end
end
